# Path to dataset root directory
path: ./datasets/shapes

# Train and validation image directories (relative to dataset root)
train: images/train
val: images/val

# Test image directory (optional)
test: images/test

# Class names and IDs
names:
  0: rectangle
  1: circle
  2: triangle